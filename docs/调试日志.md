# 2026-02-10_TrackMimic 集成与配置调整
- TrackMimic 按 BeyondMimic 训练出的轨迹条件化路径理解：控制实现仍是 BeyondMimic，只是额外接收参考轨迹输入。
- FSM 新增 `SKILL_TRACK_MIMIC`，LocoMode 增加 `SKILL_7` 路由（L1+A）。
- BeyondMimic 配置更新：`onnx_path` 指向 `GVHMR_telegram_0039`，`tau_limit_scale=1`。
- LocoMode 配置更新：`policy_path=policy_orignal.pt`，`tau_limit_scale=1`，`cmd_deadzone=0`，`lin_vel_x` 范围收敛到 `[-0.4, 0.7]`。
- 补齐 `policies/imu_calib` 与 `policies/joint_zero_check` 代码与配置（纳入版本控制）。

# 2026-02-10_Mimic 暂停与冻结参考帧
- 新增 `FSMCommand.PAUSE` 与 `state_cmd.pause`，仅在 `SKILL_BEYOND_MIMIC` / `SKILL_TRACK_MIMIC` 生效（方向键上触发）。
- 暂停时**冻结参考帧**（`ref_joint_pos/vel` 与 `ref_body_quat_w`），`counter_step` 不推进，但推理继续运行，观测仍使用实时机器人状态。
- FSM 暂停时不切模式、屏蔽 `checkChange()`，解除暂停后恢复正常流程。
- Xbox D-pad 使用 hat 映射，支持 `UP` 释放事件触发暂停。

# 2026-02-10_Mimic 完成判断与暂停切换
- Mimic 动作完成判定改为基于 `motion_length`（`counter_step` 达到长度后进入 `SKILL_COOLDOWN`）。
- ONNX metadata 若提供 `motion_length`/`motion_len`/`traj_length`，优先生效（否则继续使用 YAML）。
- 暂停状态下收到模式切换命令会自动退出暂停，允许正常切换。

# 2026-02-11_安全机制输出与 LocoMode 映射说明
- 安全机制新增 clamp 汇总输出（`clamp_summary`）与阻尼触发原因提示（`force_damping`），并增加 `log_clamps` / `log_interval_steps` 配置项。
- `configs/simulation/safety.yaml` 默认开启 `log_events/log_clamps`，便于定位动作/增益被限幅导致的响应迟钝。
- `LocoMode_lowKp.yaml` 与 `LocoMode.yaml` 补充 `kps/kds` 的 policy 顺序与电机索引映射注释，方便对照。
